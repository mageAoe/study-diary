import Vue from 'vue'
import Router from 'vue-router'
// import globa from '@/components/globa'

Vue.use(Router)

export default new Router({
  routes: [
    {
      path: '/',
      name: 'globa',
      // component: globa
    }
  ]
})
